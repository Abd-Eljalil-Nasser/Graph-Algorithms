# Connected Components
The vertices of a any graph G can be partitioned into Connected Components so that v is reachable from w if and only if they are in
the same connected component.  
so two vertices are reachable from each other if they are in the same connected component